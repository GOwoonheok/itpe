{
    "name": "itpe-flash",
    "version": "1.0.0",
    "private": true,
    "description": "ITPE Flash — 정보관리기술사 학습 PWA",
    "type": "module",
    "engines": {
        "node": ">=20.0.0"
    },
    "scripts": {
        "check": "node scripts/check.mjs",
        "test": "node --test \"tests/**/*.test.mjs\""
    },
    "dependencies": {
        "ai": "^5.0.0",
        "@ai-sdk/openai": "^2.0.0",
        "@ai-sdk/google": "^2.0.0",
        "zod": "^3.23.0",
        "aws4fetch": "^1.0.20"
    }
}
