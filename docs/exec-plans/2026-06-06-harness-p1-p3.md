# exec-plan: 하네스 엔지니어링 P1~P3 구축

- 날짜: 2026-06-06
- 배경: [../reports/2026-06-06-하네스-엔지니어링-방향-보고서.md](../reports/2026-06-06-하네스-엔지니어링-방향-보고서.md)
- 제약: **앱 동작 변경 0** — 코드(`js/`, `api/`, `*.html`, `css/`) 무수정. 하네스 층만 추가.

## 계획

| # | 작업 | 산출물 |
|---|---|---|
| P1 | 컨텍스트 문서 | AGENTS.md, CLAUDE.md(@참조), ARCHITECTURE.md, SECURITY.md, docs 체계(product-specs·exec-plans·references·reports), QUALITY.md |
| P2 | 검증 루프 | scripts/check.mjs(구문+데이터), scripts/hook-check.mjs(편집 즉시 검사), tests/data.test.mjs, npm scripts(check·test), .claude/settings.json 훅·권한 정비 |
| P3 | 가드레일 | .github/workflows/ci.yml (main 푸시·PR 시 check+test) |

## 결과 (완료 후 추기)

- `npm run check`: **JS 구문 25/25 통과, 데이터 오류 0·경고 0** ✓
- `npm test`: **2/2 통과** (데이터 무결성, 전 파일 구문) ✓
- 앱 코드 변경: **0건** (검증: `git diff --stat` 에 js/api/html/css 없음)
- 발견 사항: 기존 `.claude/settings.json` 이 존재하지 않는 `npm run typecheck` 를
  허용하고 있었음 → 실제 명령(check·test)으로 교체.
- 데이터 기준선: 현재 11개 단원 카드 데이터는 무결성 오류·경고 0 — 깨끗한 상태에서 가드 시작.

## 남은 과제 (차기 exec-plan)

- P4: Playwright E2E 3흐름(로그인→단원 진입→찾기 점프) + 스크린샷 증빙 자동화
- P5: flash.js 모듈 분할 + JSDoc 타입 검사 — **동작 동일성 검증 체계(P4) 구축 후 착수**
  (동작 변경 금지 제약상, E2E 가 먼저 있어야 분할 안전성을 증명 가능)
